# Auditoría M4 — ronda 3 (`06e96c7`)

Fecha: 2026-09-25. Base: `main` = `06e96c7` (tras los #148 a #153). Protocolo: `.claude/skills/auditoria-m4/`.

**Reparto de trabajo.** Primera ronda con ejecutor externo: Qwen 3.8 corrió el arnés y devolvió la salida literal; Opus eligió los guards, escribió los mutantes y los tests, y juzgó los resultados. Cada cifra que reportó Qwen se contrastó con los `res_<ID>.json` que deja el arnés: **coinciden las 31** (PF-4 + 11 + 14 + 5).

## Paso 1 — suite completa, antes de tocar nada

- `wt-m4r3` sobre `06e96c7`, con `real_examples/` enlazado y `CONY_test.csv` copiado: **1156 passed, 2 skipped, 3 deselected**, 0 failed. Coincide con la línea local de `CLAUDE.md`. **MEDIDA**.
- Los 2 skips: `test_logic.py:302` (no hay datos IB reales en esa ruta) y `test_logic.py:1404` (el glob de fixtures distingue mayúsculas y los archivos están en minúscula). Son los mismos 2 de la base.
- **Un worktree no hereda lo que ignora git.** Sin `real_examples/` la base dio 1065 passed / 74 skipped; con `real_examples/` pero sin `CONY_test.csv` (está en `.gitignore` por `*.csv`), 1155 / 3. Qwen paró las dos veces antes de medir mutantes, como pide el §1 del skill. Propuesta: añadir al §0 del skill que el worktree necesita los dos.

## Calibración del ejecutor (PF-4)

PF-4 (la llamada a `_politica_fiscal` olvida el cierre fiscal) sobre `06e96c7`: **2 rojos por aserción**, que son los dos tests del #149. Sobre `e53bd97` habría sido 0. Qwen lo midió bien; en el primer intento entregó expectativas («espera: …») en lugar de salidas y se le exigió la salida literal.

## Hallazgos

### R3-H1 · Un solo año cerrado con retención no usaba su cierre fiscal (Regla 4b) — PRODUCCIÓN, CERRADO en el #154

MÓDULO · `logic._refund_total_al_cobro` (lo comparten la devolución estimada de `build_tax_summary` y la casilla 9 de `_roc_refund_recuperable`).
HALLAZGO · La rama por año exigía `len(years_wh) > 1`. Con retención en un solo año caía al %ROC agregado del holder (19a) aunque ese año tuviera cierre.
EVIDENCIA · Sintético, MSTY con retención solo en 2025 ($1,000 bruto, $300 retenidos, tasa 30%): **$235.20** (78.40%) contra **$300.00** con el cierre al 100%. **MEDIDA**. En los 3 casos reales: **$0**, ninguna posición tiene un solo año con retención. **MEDIDA** (listado de `withheld_at_payment_by_year` por posición).
BASE/MOM./MUNDO · retención al cobro / tras reclasificación anual / cartera real.
VEREDICTO · Defecto de producción. Arreglado con aprobación de Daniel en el #154: un único año con fuente `cierre` también va por año. Test `test_un_solo_anio_cerrado_con_retencion_usa_su_cierre_fiscal`, rojo por aserción sin el arreglo (`235.2 == 300.0`).
Efecto colateral: 7 tests usaban «MSTY» como ticker de relleno con un %ROC inventado y la rama nueva les hacía leer el cierre real. Pasaron a `ZZZY` sin tocar ninguna aserción, y siguen verdes sobre el código anterior (112 passed). Es la filtración que ya advierte `roc_pct_by_year`: un test sintético que arrastra el yaml de producción.

### R3-H2 · Seis mutantes sobreviven a la suite en zonas que ninguna ronda había tocado

HALLAZGO · SF-1, SF-2 (`_cumul_split_factor`), TP-1, TP-2 (`_net_transfer_pairs`), WT-2 (`withheld_tax_total_by_year`) y RF-3 (borde del centavo en `_refund_total_al_cobro`): **0 rojos** en la suite completa. **MEDIDO**.
EVIDENCIA · `_cumul_split_factor` no tenía ningún test directo. Los de `_net_transfer_pairs` solo cubrían el par del mismo día y la salida sin gemela. El único test del impuesto extranjero (ZIM) depende de `real_examples/` y mira el total, no el desglose por año.
MATERIALIDAD · **$0 en los 3 casos reales** para los 6: acciones, capital, retención por año y devolución idénticas al centavo. **MEDIDA**, comparando solo cifras que no dependen del precio: una primera pasada que incluía el valor de mercado mostraba diferencias que eran ruido del precio en vivo de Yahoo (RF-3 y WT-2 «movían» el valor de mercado, cosa imposible). Dos corridas base seguidas dan ruido cero en esas cifras.
Riesgo latente, **ESTIMADO** por lectura del código: SF-2 dejaría a un cliente con dos splits posteriores a la compra (NVDA 2021 y 2024) con una cuarta parte de sus acciones; TP-1 borraría una salida real de acciones; WT-2 sumaría el impuesto israelí a la retención de EE.UU. ($2.74 en el caso real de ZIM).
VEREDICTO · Huecos de test, no defectos activos. SF-1, SF-2, TP-1, TP-2 y WT-2 se cierran con 5 tests en el PR de tests. RF-3 queda abierto a propósito: solo cambia algo con retenciones anuales de menos de un centavo.

### R3-H3 · Los huecos de la ronda 2 están cerrados, casi todos con un solo test

Los 11 supervivientes de la ronda 2 con test nuevo (CG-1, CG-3b, CG-4, CG-5, CG-6, CR-4, CR-6, O-4, H-3, H-4, H-6) mueren **por aserción** sobre `06e96c7`, ya con los datos reales montados. **MEDIDO**. Pero **9 de 11 los caza un solo test**, el dedicado del #149. Aceptable según el §3 del skill (tests dedicados), sin acción. H-3, que en la ronda 2 no se pudo medir, ahora lo cazan 2 tests.

### Nota sobre RF-2 y DR-1

Su test objetivo sigue verde, pero la suite los caza (RF-2: 6 rojos, 2 por excepción; DR-1: 21 rojos, 7 por excepción). El objetivo estaba mal elegido, no el guard. Las muertes por excepción se anotan aparte, como pide el skill.

## M4 — tabla de mutantes

**Tanda A — regresión de la ronda 2** (sobre 1156; todos por aserción):

| Mutante | Objetivo | Suite |
|---|---|---|
| PF-4 la llamada olvida el cierre fiscal | ✅ aserción | 2 |
| CG-1 split en el CSV no reescala la antigüedad | ✅ aserción | 1 |
| CG-3b corte con `>` (no realizado) | ✅ aserción | 1 |
| CG-4 la venta no se lleva su antigüedad | ✅ aserción | 2 |
| CG-5 traspaso de salida no baja la base ajustada | ✅ aserción | 1 |
| CG-6 ROC del día de la venta queda para después | ✅ aserción | 1 |
| CR-4 un cero medido se publica como «sin dato» | ✅ aserción | 1 |
| CR-6 casilla 9 sin gate de reconciliación | ✅ aserción | 1 |
| O-4 el efectivo no distributivo sale del IRR | ✅ aserción | 1 |
| H-3 fallo del guard independiente atribuido al export | ✅ aserción | 2 |
| H-4 aviso de huérfanas sin exigir la identidad | ✅ aserción | 1 |
| H-6 la fuente acepta cualquier dividendo | ✅ aserción | 1 |

**Tanda B — zonas nuevas** (sobre 1156):

| Guard | Mutante | Objetivo | Suite |
|---|---|---|---|
| Split | SF-1 split del mismo día reescala la compra | ❌ vive | **0** → R3-H2 |
| | SF-2 solo cuenta el último split | ❌ vive | **0** → R3-H2 |
| Traspasos | TP-1 empareja salida y entrada de otro día | ❌ vive | **0** → R3-H2 |
| | TP-2 una entrada empareja con varias salidas | ❌ vive | **0** → R3-H2 |
| TIR | X-1 devuelve 0.0 en vez de None | ✅ aserción | 1 |
| Devolución ROC | RF-1 sin piso de cero | ✅ aserción | 3 |
| | RF-2 dos años usan el %ROC agregado | ❌ vive en el objetivo | 6 (4 aserción, **2 excepción**) |
| | RF-3 borde del centavo `> 0` | ❌ vive | **0** (sub-centavo, abierto) |
| Ecuación Nugget | NB-1 ROC en % tratado como fracción | ✅ aserción | 1 |
| Retención | WT-1 `abs()`: el reembolso cuenta como retención (bug original) | ✅ aserción | 27 |
| | WT-2 impuesto extranjero en la retención por año | ❌ vive | **0** → R3-H2 |
| DRIP | DR-1 la fuente es cualquier dividendo | ❌ vive en el objetivo | 21 (14 aserción, **7 excepción**) |
| | DR-2 la retención de lo reinvertido se resta dos veces | ✅ aserción | 7 |
| Salud del NAV | HS-1 histéresis 45/55 invertida | ✅ aserción | 2 |

**Resultado:** de 14 mutantes nuevos, **6 sobreviven a la suite completa** (43%) y otros 2 mueren con un único test (X-1, NB-1). La tasa alta es por diseño: la tanda B apuntó a código sin test directo. No es una métrica global del repo.

## Tests añadidos

**PR del arreglo (#154):** `test_un_solo_anio_cerrado_con_retencion_usa_su_cierre_fiscal`.

**PR de tests (rama `test/m4-ronda3-huecos`, `0db8197`):**

| Mutante | Test nuevo | Con el mutante |
|---|---|---|
| SF-1 | `test_logic.py::test_split_factor_corte_por_un_dia_a_cada_lado` | ✅ `2.0 == 1.0` |
| SF-2 | `test_logic.py::test_split_factor_multiplica_todos_los_splits_posteriores` | ✅ `10.0 == 40.0` |
| TP-1 | `test_logic.py::test_net_transfer_pairs_no_empareja_una_salida_con_una_entrada_de_otro_dia` | ✅ aserción |
| TP-2 | `test_logic.py::test_net_transfer_pairs_una_entrada_empareja_con_una_sola_salida` | ✅ `0 == 1` |
| WT-2 | `test_tasa_aplicada.py::test_la_retencion_por_anio_deja_fuera_el_impuesto_extranjero` | ✅ `32.11 == 30.0` |

Esperados anclados a literales (40 = 4 × 10; $30 de la fila NRA), nunca a la función auditada. El test de WT-2 es de Regla 3b: compara el desglose por año con el total.

**Verificación con el arnés (tanda C, sobre `0db8197`, base 1162 / 2 skips):** los 5 mueren **por aserción**, 0 por excepción, archivos restaurados. **MEDIDO**. Cada uno lo caza exactamente 1 test, el nuevo dedicado; no hay rojos fuera de él.

Suite local con los 5 tests: **1162 passed, 2 skipped, 3 deselected**, 0 failed. `CLAUDE.md` actualizado en el mismo PR.

## Supuestos y materialidad

- Materialidad medida sobre los 3 casos reales (`ib`, `schwab`, `schwab2`), con cifras independientes del precio. El valor de mercado queda fuera porque el precio en vivo mete ruido entre corridas.
- El riesgo latente de R3-H2 está **ESTIMADO**: ningún caso real tiene dos splits posteriores, traspasos de distinto día ni impuesto extranjero en un año con retención NRA.
- R3-H1 se midió con un caso sintético; con datos reales el efecto es $0.

## Nota: **7/10** al empezar, **8.5/10** al cerrar

- Ronda 1: 8/10. Ronda 2: 6/10 al empezar, 7.5/10 al cerrar.
- Lo que la sube: los 11 huecos de la ronda 2 siguen cerrados sobre `main`; el núcleo fiscal medido (WT-1, RF-1, DR-2, HS-1, NB-1) caza sus mutantes, WT-1 con 27 tests.
- Lo que la bajaba al empezar: un defecto de producción en la Regla 4b (R3-H1) y 6 supervivientes en código de ingesta (splits, traspasos) sin un solo test directo.
- Lo que queda: RF-3 abierto (sub-centavo), 9 guards con un único test y 2 más en la tanda B (X-1, NB-1).

## Apéndice — diff exacto de cada mutante de la tanda B

Los de la tanda A son los de la ronda 2, con el mismo diff (ver `2026-09-25-m4-ronda2-e53bd97.md`). Cada uno se aplicó con exactamente 1 ocurrencia sobre `06e96c7`.

**SF-1** · `logic.py` · split del mismo día que la compra también la reescala (> por >=)
```diff
-        future = splits_series[idx.normalize() > tx_ts]
+        future = splits_series[idx.normalize() >= tx_ts]
```

**SF-2** · `logic.py` · con dos splits posteriores solo cuenta el último
```diff
-        for r in future:
-            factor *= float(r)
+        for r in future:
+            factor = float(r)
```

**TP-1** · `logic.py` · el par de traspaso se empareja aunque sea de otro día (una salida real desaparece)
```diff
-            if (out.at[j, 'Ticker'] == t and out.at[j, 'Date'] == d
+            if (out.at[j, 'Ticker'] == t
```

**TP-2** · `logic.py` · una entrada empareja con varias salidas
```diff
-                in_idx.remove(j)  # cada entrada empareja con una sola salida
+                pass
```

**X-1** · `logic.py` · xirr devuelve 0.0 en vez de None cuando no hay respuesta
```diff
-    if not any(v < 0 for _, v in puntos) or not any(v > 0 for _, v in puntos):
-        return None
+    if not any(v < 0 for _, v in puntos) or not any(v > 0 for _, v in puntos):
+        return 0.0
```

**RF-1** · `logic.py` · devolución estimada sin piso de cero (retención justa > retenido => devolución negativa)
```diff
-    refund = max(0.0, min(withheld, withheld - fair))
+    refund = withheld - fair
```

**RF-2** · `logic.py` · con dos años de retención se usa el %ROC agregado en vez del de cada año (> 1 por > 2)
```diff
-    if len(years_wh) > 1:
-        rby = estimate_roc_refund_by_year(
+    if len(years_wh) > 2:
+        rby = estimate_roc_refund_by_year(
```

**RF-3** · `logic.py` · un año con 1 centavo de retención cuenta como año con retención (rama hermana del centavo)
```diff
-    years_wh = sorted(y for y, v in wap_by_year.items() if v > 0.01)
+    years_wh = sorted(y for y, v in wap_by_year.items() if v > 0)
```

**NB-1** · `logic.py` · ecuación Nugget con el ROC en % tratado como fracción
```diff
-    effective = round(base * (1 - roc / 100.0), 2)
+    effective = round(base * max(0.0, 1 - roc), 2)
```

**WT-1** · `logic.py` · retención total suma abs() y cuenta el reembolso como más retención (bug original $105+$75=$180)
```diff
-        signed += float(amt)
-    return round(max(0.0, -signed), 2)  # retención neta soportada (≥0)
+        signed -= abs(float(amt))
+    return round(max(0.0, -signed), 2)  # retención neta soportada (≥0)
```

**WT-2** · `logic.py` · retención por año cuenta también el impuesto extranjero (predicado incoherente)
```diff
-        signed[y] = signed.get(y, 0.0) + float(amt)
-    for y, s in signed.items():
-        out[y] = round(max(0.0, -s), 2)  # retención neta del año (≥0)
+        signed[y] = signed.get(y, 0.0) + float(amt)
+    for y, s in signed.items():
+        out[y] = round(max(0.0, -s), 2) + foreign_tax_paid_by_year(history_df).get(y, 0.0)
```

**DR-1** · `logic.py` · retención de lo reinvertido: la fuente es cualquier dividendo del día (no solo el reinvertido)
```diff
-    es_fuente_drip = (accion.str.contains('reinvest|reinversión|drip')
-                      & accion.str.contains('dividend|dividendo')
+    es_fuente_drip = (accion.str.contains('dividend|dividendo')
```

**DR-2** · `logic.py` · la retención de lo reinvertido se resta dos veces del efectivo (se ignora la atribución)
```diff
-        _retencion_en_efectivo = _tax_totals_early['withheld'] - _withheld_on_reinvested(ticker_df)
+        _retencion_en_efectivo = _tax_totals_early['withheld']
```

**HS-1** · `logic.py` · histéresis invertida: se sale de destructivo con 55 y se entra con 45
```diff
-    if prev_verdict == 'destructive':
-        roc_high = roc_pct >= ROC_HEALTH_HIGH_EXIT_PCT
-    elif prev_verdict in ('accounting', 'mixed'):
-        roc_high = roc_pct >= ROC_HEALTH_HIGH_ENTER_PCT
+    if prev_verdict == 'destructive':
+        roc_high = roc_pct >= ROC_HEALTH_HIGH_ENTER_PCT
+    elif prev_verdict in ('accounting', 'mixed'):
+        roc_high = roc_pct >= ROC_HEALTH_HIGH_EXIT_PCT
```
